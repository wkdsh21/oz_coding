# a=input(), a=input("입력해주세요")
# int(), float() ,str() , bool() ,tuple(), list(), dict(), set()
# input().split() 띄어쓰기로 구분 input().split(".")  '.' 으로 구분
# map(int, input().split(".")) map(함수, 시퀀스자료형)
# print(a,b,c,d,sep='/') 구분자를 넣을수있음
# print(a,b,c,d, end=' ') 끝에 엔터를 공백으로 대체
